#include <stdio.h>
#include <unistd.h>

/**
 *main - Prints the alphabet in lower case\n
 *
 *
 *Return: Always 0.
*/
int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
return (0);
}
